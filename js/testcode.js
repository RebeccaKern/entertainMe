        //$("resultsInfo").push(info);
        //$(div).create
        //$("#similarArtistResults").append(similarMusic[i].Name);//("<div id=i>similarMusic[i].Name</div>");
        //$("#similarArtistResults").append(similarMusic[i].wTeaser);
        //$("#similarArtistResults").append(similarMusic[i].yUrl);
        //$("#similarArtistResults").append(resultsInfo[i][0]);

            // for (var i = 0; i < similarMusic.length; i ++){
    //     $("#similarArtistResults").append(resultsInfo[i][0]);
    // }
    // for (var i = 0; i < similarMusic.length; i ++){
    //     // var info = [similarMusic[i].Name, similarMusic[i].wTeaser, similarMusic[i].yUrl];
    //     // $("resultsInfo").append(info);
    //     $("#similarArtistResults").append(resultsInfo[i][0]);
    //     $("#similarArtistResults").append("<br>");

        // for (var k = 0; k < similarMusic.length; i ++){
    //     // var info = [similarMusic[i].Name, similarMusic[i].wTeaser, similarMusic[i].yUrl];
    //     // $("resultsInfo").append(info);
    // }


    $( "#1" ).click(function() {
      alert( "Handler for .click() called." );
    });

    $("#1").click(function(){
        $("#1").append("hello world");
        //$("#1").append(resultsInfo[i][1]);
    })

    $('#1').on( "click", function() {
        alert(1);
    });

    //alert(resultsInfo[i]);

    //     function showstuff(myVal) {
    //     var x = "<p>The new text is " + resultsInfo[i][1] + "</p>";
    //     $('#similarArtistResults').append(x);
    // }

// jQuery(document).ready( function($){
//     $( "#target" ).click(function() {
//       alert( "Handler for .click() called." );
//     })

// };

     // div.addListener('click', function (event) {
     //     alert('Hi!');
     // });
    // $('#1').on( "click", function() {
    //     alert(1);
    // });


// })

            //             tag.onclick = function(){
    // alert('you clicked ' + j);
                // var $this = $(this);
                //     if($this.data('clicked')) {
                //         $(extraInfo).toggleClass("hide");
                //         //$(tag).append(resultsInfo[i][1]);
                //     }

            // fav.onclick = function(){
            //     isFav = "#".concat("heart".concat(k.toString()));
            //     f = document.getElementById("heart".concat(k.toString()));
            //     if (f.className == "faFullHeart"){
            //         $(isFav).addClass("faEmptyHeart");
            //         $(isFav).removeClass("faFullHeart");
            //     }
            // }